networks:
  proxy:
    external: true
services:
  actualbudget:
    healthcheck:
      test: ['CMD-SHELL', 'node src/scripts/health-check.js']
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 20s
    image: ghcr.io/actualbudget/actual:${actualbudget_version:-latest}
    deploy:
      labels:
        homepage.group: Personal
        homepage.href: https://ab.buncha.men
        homepage.icon: sh-actual-budget.svg
        homepage.name: Actual Budget
        kuma.__docker:
        release_notes: "https://github.com/actualbudget/actual/releases"
        traefik.enable: "true"
        traefik.http.routers.actualbudget.entryPoints: "https"
        traefik.http.routers.actualbudget.middlewares: "tinyauth@file,localonly@file"
        traefik.http.routers.actualbudget.rule: "Host(`ab.buncha.men`)"
        traefik.http.services.actualbudget.loadbalancer.server.port: 5006
    networks:
      proxy: null
    volumes:
      - /mnt/docker/actualbudget:/data

# Utils

This project provides a few file and text sharing utilities.

For privatebin to work properly, the folders must be created and owned by the user and group
specified with the `uid` and `gid` variables.

services:
  base:
    deploy:
      restart_policy:
        condition: on-failure
    logging:
      driver: local
      options:
        compress: "true"
        max-file: "3"
        max-size: "10m"
    stdin_open: false
    tty: false

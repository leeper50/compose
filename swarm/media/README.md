# Media Stack

## Seerr
The folder under `/mnt/docker/seerr` must be owned by a user with with id 1000 and group id 1000.

Run `chown -R 1000:1000 <your_path_here>/seerr` to ensure that this is true.
